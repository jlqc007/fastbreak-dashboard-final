import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from 'sonner'

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fastbreak Dashboard - Sports Event Management",
  description: "Manage your sports events efficiently with Fastbreak Dashboard. Create, edit, and track basketball, soccer, volleyball, football, and baseball events.",
  keywords: ["sports", "event management", "dashboard", "basketball", "soccer", "volleyball", "football", "baseball"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
        <Toaster />
      </body>
    </html>
  )
}
